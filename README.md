### USEAGE

#### Message
```
Message.open('what');
Message.info('is');
Message.warn('your');
Message.error('name');
Message.success('?');

# default duration 2
Message.config({
    duration: 3
})
```

#### Img
the same as img element, only add click event